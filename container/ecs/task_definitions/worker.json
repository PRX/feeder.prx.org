{
  "family": "$BASE_NAME-worker-${ENV:-staging}",
  "containerDefinitions": [{
    "name": "$BASE_NAME-worker",
    "image": "$REPO_STRING",
    "cpu": $CPU,
    "memory": $MEMORY,
    "links": [],
    "portMappings": [],
    "essential": true,
    "entryPoint": [],
    "command": ["worker"],
    "environment": $ENV_JSON,
    "mountPoints": [],
    "volumesFrom": []
  }],
  "volumes": []
}
