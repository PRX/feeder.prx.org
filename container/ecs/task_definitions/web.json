{
  "family": "$BASE_NAME-web-${ENV:-staging}",
  "containerDefinitions": [{
    "name": "$BASE_NAME-web",
    "image": "$REPO_STRING",
    "cpu": $CPU,
    "memory": $MEMORY,
    "links": [],
    "portMappings": [{
      "hostPort": $HOST_PORT,
      "containerPort": $CONTAINER_PORT,
      "protocol": "tcp"
    }],
    "essential": true,
    "entryPoint": [],
    "command": [],
    "environment": $ENV_JSON,
    "mountPoints": [],
    "volumesFrom": []
  }],
  "volumes": []
}
