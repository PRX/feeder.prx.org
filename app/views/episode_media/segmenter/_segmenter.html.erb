<%= render "waveform/waveform",
      ad_breaks: episode.ad_breaks,
      ad_breaks_valid: episode.validate(:ad_breaks),
      duration: uncut&.duration,
      form: form,
      segmentation: uncut&.sanitize_segmentation,
      url: uncut&.url,
      waveform_url: uncut&.waveform_url %>
