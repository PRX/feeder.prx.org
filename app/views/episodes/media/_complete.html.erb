<div class="col-12 mb-4 prx-field-group">
  <div class="form-floating">
    <div class="form-control d-flex align-items-center prx-audio-player" data-controller="audio-player">

      <button type="button" class="btn p-0 prx-play" data-action="audio-player#play">
        <span class="material-icons text-primary mx-2 my-1">play_arrow</span>
      </button>

      <button type="button" class="btn p-0 prx-pause" data-action="audio-player#pause">
        <span class="material-icons text-primary mx-2 my-1">pause</span>
      </button>

      <div class="text-secondary me-2"><%= media.file_name %></div>

      <small class="text-danger flex-grow-1 me-2"><%= t(".unable") %></small>

      <div class="flex-grow-1 me-2 py-2 prx-scrub" data-action="mousedown->audio-player#mouseDown" data-audio-player-target="scrubber">
        <div class="progress" data-audio-player-target="progress">
          <div class="progress-bar" role="progressbar" style="width: 0%;" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" data-audio-player-target="progressBar"></div>
        </div>
      </div>

      <audio class="d-none" src="<%= media.href %>" preload="none" data-audio-player-target="audio" data-action="timeupdate->audio-player#audioTimeUpdate ended->audio-player#audioEnded"></audio>

      <small class="text-muted" data-audio-player-target="duration">(<%= episode_media_duration(media) %>)</small>

      <%= link_to edit_episode_path(media.episode, uploads_destroy_params(form)), class: "btn" do %>
        <span class="material-icons">delete</span>
      <% end %>
    </div>

    <label><%= episode_media_label(media) %></label>
  </div>

  <button type="button" class="prx-btn-help" data-bs-toggle="modal" data-bs-target="#media-modal-<%= media.id %>">
    <span class="material-icons">info</span>
  </button>
</div>

<%= render "episodes/media/modal", episode: episode, media: media, id: "media-modal-#{media.id}" %>
