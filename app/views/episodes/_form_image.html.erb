<div class="card">
  <div class="card-header-info">
    <h5 class="card-title"><%= t(".title") %></h5>
  </div>
  <div class="card-body">
    <% if episode.image %>
      <%= image_tag(episode.image.url, width: 135, alt: episode.image.alt_text, class: "d-flex mx-auto mb-2") %>
    <% else %>
      <h3>No image here</h3>
    <% end %>

    <p>tktktk</p>
  </div>
</div>
