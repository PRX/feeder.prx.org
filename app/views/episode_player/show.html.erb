<% content_for :title, "#{@episode.title} - #{t(".title")}" %>

<%= render "episodes/tabs" %>

<%= turbo_frame_tag "episode-player", data: {controller: "morphdom"} do %>
  <%= render "form", episode: @episode, player_options: @player_options %>
<% end %>
