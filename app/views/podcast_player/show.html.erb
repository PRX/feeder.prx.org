<% content_for :title, "#{@podcast.title} - #{t(".title")}" %>

<%= render "podcasts/tabs" %>

<%= turbo_frame_tag "podcast-player", data: {controller: "morphdom"} do %>
  <%= render "form", podcast: @podcast, player_options: @player_options %>
<% end %>
