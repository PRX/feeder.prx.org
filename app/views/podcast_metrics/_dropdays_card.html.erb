<%= turbo_frame_tag "dropdays" do %>
  <div class="row d-flex flex-wrap gx-4"
    data-controller="apex"
    data-apex-id-value="<%= SecureRandom.uuid %>"
    data-apex-series-type-value="dropdays"
    data-apex-interval-value=""
    data-apex-series-data-value="<%= episode_dropdays.to_json %>"
    data-apex-date-range-value="">
    <div class="col">
      <div data-apex-target="chart" data-chart="dropdays"></div>
    </div>
    <div class="col align-items-center">
      <div class="col my-4">
        <table class="table table-striped shadow rounded">
          <thead>
            <tr class="table-secondary">
              <th>Episode</th>
              <th>Publish Date</th>
              <th>7 day Downloads</th>
              <th>All-time</th>
            </tr>
          </thead>
          <tbody>
            <% episode_dropdays.each_with_index do |ep, i| %>
              <% sum = sum_rollups(ep[:rollups]) %>
              <% total = sum_rollups(ep[:totals]) %>
              <% title = ep[:ep][:title] %>
              <tr class="align-items-center">
                <td class="d-flex align-items-center">
                  <input id="<%= title %>" type="checkbox" class="form-check-input mt-0 me-2" checked="true" data-action="click->apex#toggleSeries" data-series="<%= title %>" data-apex-target="episodebox">
                  <span class="material-icons me-2" aria-label="hidden" style="color: <%= line_chart_colors[i] %>">circle</span>
                  <span class="text-nowrap overflow-hidden"><%= title %></span>
                </td>
                <td><%= ep[:ep][:published_at].strftime("%Y-%m-%d") %></td>
                <td><% if sum %><%= sum %><% else %>&mdash;<% end %></td>
                <td><% if total %><%= total %><% else %>&mdash;<% end %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <div class="col my-4">
          <%= paginate episodes, param_name: :episode_dropdays %>
        </div>
      </div>
    </div>
  </div>
  </div>
<% end %>
