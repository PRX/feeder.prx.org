<%= render "podcasts/tabs" %>

<%= form_with(url: podcast_planner_path(@podcast), html: {method: :get}) do |form| %>
  <div class="row mt-4">
    <%= render "form_main", form: form %>
    <%= render "form_side", form: form %>
  </div>
<% end %>
